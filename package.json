{
  "name": "socketspeak",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "install-all": "npm install && cd frontend && npm install && cd ../backend && npm install",
    "build": "cd frontend && npm install && npm run build",
    "start": "cd backend && npm install && NODE_ENV=production npm start",
    "dev": "cd backend && npm run dev"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
